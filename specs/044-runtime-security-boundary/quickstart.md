# Quickstart: Runtime Security Boundary

1. Read `docs/runtime-observations.md`.
2. Validate the sample runtime observation fixture selected during implementation.
3. Run a map with runtime input if supported by the final command shape.
4. Inspect graph/map output and confirm runtime facts are `runtime-visible`.
5. Read `docs/security-threat-model.md` and verify each threat has a state.
